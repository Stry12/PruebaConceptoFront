# Heurísticas promovidas

Lecciones de diseño validadas en ≥2 proyectos/iteraciones distintas. Una observación de un solo proyecto NO entra aquí — vive en `projects/<slug>.md` hasta que se repita. Solo escribe `art_director`, durante la consolidación (ver skill `design-intelligence`).

> Reset 2026-07-19: base vaciada a petición del usuario (partida desde cero). Las candidatas del run anterior fueron eliminadas junto con su proyecto de origen.

Formato de entrada (append-only; una heurística no se borra, se marca `superada por:` si otra la reemplaza):

```markdown
## H-<n>: <título corto>
- **Regla:** <la heurística, accionable en una frase>
- **Evidencia:** <proyecto/iteración donde se observó, con delta de score si aplica — mínimo 2 ocurrencias>
- **Aplica a:** <vertical/arquetipo/registro, o "general">
- **Consumidores:** <brand_strategist | color_strategist | ui_designer | art_director | prompt refinement>
- **Fecha:** <YYYY-MM-DD>
```

---

_(sin heurísticas promovidas — se requieren ≥2 ocurrencias en proyectos distintos)_

## Candidatas pendientes

_(ninguna — la base se reseteó; las nuevas candidatas saldrán de la consolidación de los próximos runs)_
